/*
This prevents the page from being horizontally scrollable when the logo expands. Unfortunately, overflow: hidden messes up the page.
*/
/*
window.onscroll = function () {
    window.scrollTo(0,window.scrollY);
};*/